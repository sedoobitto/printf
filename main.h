#ifndef MAIN_H
#define MAIN_H
#include <stdarg.h>
int _putchar(char c);
int _printf(const char *format, ...);
int character(va_list parameter);
int string_character(va_list parameter);
int print_digit(va_list paramter);
#endif
